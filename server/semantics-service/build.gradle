//configuration for gretty webapp server
gretty {
    contextPath '/semantics/'
}

dependencies {
    compile group: 'org.apache.jena', name: 'jena-core', version: '3.1.1'
    compile project(':semantic-core')
}

