apply plugin: 'java'
apply plugin: 'war'

repositories {
	mavenCentral()
}

gretty {
	contextPath '/auth'
}

dependencies {
	compile project(':tools-common');
    compile project(':incore-common');
}

task wrapper(type: Wrapper) {
	gradleVersion = '3.2'
}
