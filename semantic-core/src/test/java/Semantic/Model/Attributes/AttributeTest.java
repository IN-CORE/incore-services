package Semantic.Model.Attributes;

import Semantic.Model.Attributes.Enumerable.Enumeration;
import org.junit.jupiter.api.Test;

import java.util.Arrays;

public class AttributeTest {
    @Test
    public void test() {
//        EnumerationAttribute vacancy = new EnumerationAttribute("VACANCY", "Type of Vacancy", Arrays.asList(
//                new Enumeration(0, "N/A"),
//                new Enumeration(1, "For rent (H0050002)")
//        ));
//
//        isRequired = false;
//        isNullable = true;
//        UnknownValues = "N/A", "NULL";
//        isUnique = false;

        // A. Representing the User's Schema as a Model
        // B. Adding a relationship between the User's Schema and The Concept
        // C. Representing the Concept as a Model

        // We also want to state that ever user's schema is potentially a usable concept




        // so how does this match the concept of Housing Vacancy

        // Generic Concept
        // Concept
        // -> Enumeration

        // Enumerable Mapping
    }
}
